git branch <new_branch>
git checkout <new_branch>
git branch -a #check that we are on new branch

git add new_file.txt
git status #list new files or changes to project

git push #push new branch to server
git config --global user.name 'your username'
git config --global user.email
git commit -a commits all staged files

git mv <original_name> <new_name> #rename files
git rm <file_name> #delete file

nano .gitignore *~
